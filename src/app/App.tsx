import {Headline1} from '@salutejs/plasma-ui';
import React, {FC, memo} from 'react';

const App: FC = () => {
    return (
        <div>
            <Headline1>Hello</Headline1>
        </div>
    );
};

export default memo(App);
